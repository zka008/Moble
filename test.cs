using System;
namespace CodeWork
{
    internal class Program
    {
        static void Main(string[] args)
        {
            DateTime dt = DateTime.Now;
        
        }

        class 클래스이름
        {
            //생성자를 명시적으로 선언하지 않았을 경우
            //    컴파일러가 자동으로 매개변수가 없는 생성자를 생성함

            //하지만 매개변수가 있는 생성자를 하나 이상 선언했을 경우
            //    컴파일러는 매개변수가 없는 생성자를 생성하지 않음

            //때문에 매개변수가 있는 생성자를 선언했을 경우 
            //    매개변수가 없는 생성자 또한 선언해주어야 함
        }
    }
}